@* @model IEnumerable<VALCOBulkSMSAlertSystem.Models.VALCOBulkSMSAlertSystem.Models.Contacts>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<div>
    <a asp-area="" asp-controller="Contacts" asp-action="SelectedRecipients">
        Selected Recipients
    </a>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
 *@


@model VALCOBulkSMSAlertSystem.Models.ContactsIndexViewModel

@{
    ViewData["Title"] = "Index";
}

<h1>Contacts</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<div>
    <h4>Select a contact(s) to a send message(s)</h4>
</div>

@if (Model.SelectedContacts != null && Model.SelectedContacts.Count > 0)
{
    <h2>Selected Contacts:</h2>
    <ul>
        @foreach (var contact in Model.SelectedContacts)
        {
            <li>@contact</li>
        }
    </ul>
}

<form method="post" asp-controller="Contacts" asp-action="SelectRecipients">
    @*Post to URL: *@
    <table class="table">
        <thead>
            <tr>
                <th>
                    <input id="selectAll" type="checkbox" />
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ContactsList[0].Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ContactsList[0].Phone)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.ContactsList.Count; i++)
            {
                <tr>
                    <td>
                        <input name="selectedContacts" type="checkbox" value="@Model.ContactsList[i].Name" />
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => Model.ContactsList[i].Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => Model.ContactsList[i].Phone)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@Model.ContactsList[i].Id">Edit</a> |
                        <a asp-action="Details" asp-route-id="@Model.ContactsList[i].Id">Details</a> |
                        <a asp-action="Delete" asp-route-id="@Model.ContactsList[i].Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <input type="submit" value="Select" class="btn btn-primary" />
</form>

@section Scripts {
    <script>
        // Check/uncheck all checkboxes when "Select All" checkbox is clicked
        $(function () {
            $('#selectAll').click(function () {
                $('input[name="selectedContacts"]').prop('checked', $(this).prop('checked'));
            });
        });
    </script>
}

