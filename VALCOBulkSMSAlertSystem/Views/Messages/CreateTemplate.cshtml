@* 
@{
    ViewData["Title"] = "Create Template";
}

<h2>Create Template</h2>

@using (Html.BeginForm("MessageTemplate", "Home", FormMethod.Post))
{
    Template template = new Template();

    <div class="form-group">
        @Html.LabelFor(model => template.Name)
        @Html.TextBoxFor(model => template.Name, new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.LabelFor(model => template.Content)
        @Html.TextAreaFor(model => template.Content, new { @class = "form-control", rows = 5 })
    </div>

    <div class="form-group">
        <button type="submit" class="btn btn-primary" onclick="openTab(event, 'tab3-2')">Create</button>
    </div>
}

<div class="text-right">
    <button type="button" class="btn btn-secondary" onclick="insertPlaceholder('<<First Name>>')">First Name</button>
    <button type="button" class="btn btn-secondary" onclick="insertPlaceholder('<<Last Name>>')">Last Name</button>
    <button type="button" class="btn btn-secondary" onclick="insertPlaceholder('<<Full Name>>')">Full Name</button>
</div>
 *@